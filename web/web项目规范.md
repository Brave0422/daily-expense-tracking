# Web 项目规范

## 常规确认按钮的高亮颜色

- 登录、注册、重置密码、确认修改、保存和提交等常规确认按钮，在可操作的悬停态和键盘聚焦态下，背景色与边框色统一使用 `#ffe639`，文字使用能保证对比度的深色。
- 普通状态沿用按钮所在页面和组件的既有样式，不因本规范修改普通状态颜色。
- 禁用态和加载态不应用高亮颜色；删除、退出等危险操作按钮遵循对应的危险操作视觉规范。
- 使用 TDesign 主按钮实现常规确认操作时，为按钮添加 `app-confirm-button` 类，由全局样式统一维护高亮状态，避免在业务组件内重复声明颜色。
